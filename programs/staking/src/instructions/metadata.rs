//! REMOVED in v3. $STAKE now uses the Token-2022 native metadata extension set at
//! mint creation (setup script); the reward token is external. This file is no
//! longer referenced by `mod.rs` and is intentionally empty.
